import { CloudDrizzle } from "lucide-react";

const CloudDrizzleIcon = ({ color, size }: { size: string; color: string }) => {
  return <CloudDrizzle size={size} color={color} />;
};

export default CloudDrizzleIcon;
