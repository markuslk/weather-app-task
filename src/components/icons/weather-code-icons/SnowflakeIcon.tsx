import { Snowflake } from "lucide-react";

const SnowflakeIcon = ({ color, size }: { size: string; color: string }) => {
  return <Snowflake size={size} color={color} />;
};
export default SnowflakeIcon;
