import { CloudRain } from "lucide-react";

const CloudRainIcon = ({ color, size }: { size: string; color: string }) => {
  return <CloudRain size={size} color={color} />;
};
export default CloudRainIcon;
