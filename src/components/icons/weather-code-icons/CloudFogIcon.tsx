import { CloudFog } from "lucide-react";

const CloudFogIcon = ({ color, size }: { size: string; color: string }) => {
  return <CloudFog size={size} color={color} />;
};
export default CloudFogIcon;
